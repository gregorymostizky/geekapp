Adding file to <%= @paper.title %>

<%= form_for :file, @file, :url=> {:action => :create }, :html => {:multipart => true} do |f| %>
    <%= f.file_field :file %>
    <%= f.hidden_field :paper_id, :value => @paper.id %>
    <%= f.submit 'Upload' %>
<% end %>