<h2><%= @paper.title %></h2>

<hr>

<span><%= @paper.description %></span>

<hr>

<ul>
    <% @paper.sources.each do |sf| %>
        <li><%= sf.file.original_filename %> (<%= sf.file.content_type %>)</li>
    <% end %>

    <%= link_to 'Add', :controller => :files, :action => :new, :paper_id => @paper.id %>
</ul>